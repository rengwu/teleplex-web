import { Metadata } from 'next';

export default async function Home() {
  // const site = await strapiRequest(`/api/site?populate=deep&populate=*`).data;

  return <div>Hello next-strapi-static</div>;
}
